# PRD — 작가 서포트 집필 에이전트 웹앱 (가칭: **BookK Writer**)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 (2026-09-23) |
| 대상 출판 | 부크크(BOOKK) 자가출판, **A5(148×210mm)** 단행본 |
| AI 모델 | `claude-fable-5-1` (OpenAI 호환 `POST /v1/chat/completions`) |
| 사용 형태 | 1인 작가용 웹앱 (로컬 실행 우선, 추후 배포 가능 구조) |
| 관련 문서 | `instruction.md`(서술 스타일 규칙), `prompts.md`(개발·런타임 프롬프트) |

---

## 1. 제품 개요

### 1.1 한 줄 정의
작가가 **절 단위로 스케치**를 던지면, AI가 **작가의 문체·비전·집필 의도**를 반영해 **지정한 분량(페이지)**으로 본문을 서술하고, 결과물을 **부크크 A5 인쇄 규격 그대로** PDF/HWPX로 내보내는 집필 에이전트.

### 1.2 해결하려는 문제
- 목차 설계에 시간이 오래 걸리고, 독자 관점·시장 트렌드를 반영하기 어렵다.
- 초안 쓰기(빈 페이지 공포)가 가장 큰 병목이다.
- 워드프로세서에서 쓴 원고를 인쇄 규격(재단 여백·안쪽 여백·폰트 임베딩)에 맞추는 후작업이 번거롭고 반려가 잦다.
- 챕터가 늘어날수록 앞뒤 내용·용어·문체의 일관성이 무너진다.

### 1.3 목표 / 성공 지표
| 목표 | 지표 |
|---|---|
| 초안 작성 시간 단축 | 절 1개(3p) 초안 생성 ≤ 90초, 작가 수정률(생성 대비 수정 글자 비율) 추적 |
| 인쇄 규격 무결성 | 내보낸 PDF가 부크크 규격체크(판형 ±1mm, 재단 여백 3mm) 1회 통과율 100% |
| 분량 정확도 | 지정 페이지 대비 실제 조판 페이지 오차 ±15% 이내 |
| 데이터 안전성 | 자동 저장으로 인한 유실 0건 (마지막 입력 후 ≤2초 내 저장) |

### 1.4 범위 밖 (v1)
- 표지 디자인 제작(책등 계산·표지 PDF) → v2 후보
- 다중 사용자 실시간 공동 편집
- 전자책(EPUB) 내보내기 → v2 후보

---

## 2. 사용자 & 핵심 시나리오

**페르소나**: 전문 분야 지식이 있는 1인 저자(강사·실무자·에세이스트). 문체에 대한 고집이 있고, 초안은 AI에 맡기되 최종 문장은 직접 다듬고 싶어 한다.

**핵심 흐름**
1. 새 프로젝트 생성 → 책 정보(제목·저자·주제·집필 의도·대상 독자·기타) 입력, **문체 샘플** 첨부(선택)
2. AI가 **목차(장/절) 설계안 + 설계 근거 보고서**를 생성 → 작가가 수정·승인
3. 좌측 목차에서 절 클릭 → 우측 편집기 활성화 (상단에 `3장 > 2절` 네비게이션)
4. 빈 페이지에 스케치 입력 → 분량(예: 3p) 지정 → **[집필하기]**
5. AI가 스트리밍으로 본문 작성 → 작가 수정 → 자동 저장
6. **[교정·교열]** 버튼 → 해당 절 일괄 적용(변경 내역 확인·되돌리기 가능)
7. 펼침면(좌/우 페이지) 미리보기로 독자 시점 확인
8. PDF(부크크 제출용) / HWPX 다운로드

---

## 3. 인쇄 규격 (부크크 A5) — 시스템 상수

> 출처: 부크크 FAQ「풀빼다(재단 여백) 작업 안내」, 부크크 표지·내지 자동 규격체크(v1.0) 소스, 작업 폴더의 `[한글서식]A5(국판)_부크크서식(기본).hwp` 실측값.

### 3.1 판형 & 재단 여백
| 항목 | 값 |
|---|---|
| 완성(재단) 크기 | **148 × 210 mm** |
| 재단 여백(풀빼다, bleed) | **사방 3mm** |
| 작업(문서) 크기 | **154 × 216 mm** (= 148+3×2, 210+3×2) |
| 부크크 규격 허용 오차 | ±1mm (148×210 또는 154×216 중 하나와 일치해야 함) |
| 페이지 배치 | **맞쪽(mirror margin)** — 홀수 쪽 = 오른쪽 페이지(recto), 짝수 쪽 = 왼쪽 페이지(verso), 1쪽은 단독 오른쪽 |

- 이미지·배경색이 페이지 끝까지 닿는 **풀블리드 요소가 하나라도 있으면 반드시 154×216으로 출력**한다. 본 앱은 **기본값을 154×216(재단 여백 포함)** 으로 고정한다. (정사이즈 원고를 부크크가 임의 확장하면 원고가 2~4% 확대되고 가장자리가 잘리기 때문)
- 옵션으로 148×210 정사이즈 출력도 제공(풀블리드 요소가 없을 때만 활성화).

### 3.2 본문 여백 (부크크 기본 HWP 서식 실측, 재단 여백 포함 문서 기준)
| 항목 | 문서 가장자리 기준 | 재단선 기준 |
|---|---|---|
| 안쪽(제본 쪽) | 28mm | 25mm |
| 바깥쪽 | 23mm | 20mm |
| 위 | 18mm (+머리말 7mm) | 15mm → 본문 시작 22mm |
| 아래 | 18mm (+꼬리말 13mm) | 15mm → 본문 끝 28mm |

- 본문 영역: **폭 103mm × 높이 약 160mm**
- 부크크 권장 **안전 영역(최소)**: 재단선에서 위·아래·바깥 7mm, 안쪽 12mm. 중요한 글·이미지는 가장자리에서 10~15mm 이상 안쪽. → 위 기본 여백은 이를 모두 만족. 사용자가 여백을 수정하면 이 최소값 미만은 **차단(에러)** 한다.
- 쪽 번호: 꼬리말 영역, 바깥쪽 정렬(홀수 쪽 오른쪽 하단 / 짝수 쪽 왼쪽 하단), 9pt.

### 3.3 조판 기본값
| 항목 | 값 |
|---|---|
| 본문 글꼴 | **KoPub바탕체 Light** (편집기·미리보기·PDF·HWPX 공통 강제 적용) |
| 제목 글꼴 | KoPub돋움체 Medium (장·절 제목, 선택 변경 가능) |
| 본문 크기 / 줄 간격 | 10pt / 160% (부크크 서식 기준) |
| 정렬 | 양쪽 정렬, `word-break: keep-all`(어절 단위 줄바꿈), 문단 첫 줄 들여쓰기 1em |
| 과부/고아 줄 | 최소 2줄 (`orphans: 2; widows: 2`) |
| 이미지 해상도 | 인쇄 크기 기준 **300 DPI 이상** 권장, 미만이면 경고 |
| 글꼴 임베딩 | PDF에 서브셋 임베딩 필수 |

### 3.4 분량 ↔ 페이지 환산
- 기준: 본문 영역 103×160mm, 10pt/160% → 약 **28줄 × 29자 ≈ 페이지당 650~750자(공백 포함)**.
- 초기 환산계수 `CHARS_PER_PAGE = 700`. 실제 조판 결과(페이지네이션 엔진)로 프로젝트별 자동 보정(이동 평균).
- 이미지는 차지하는 높이만큼 글자 수에서 차감.

---

## 4. 기능 요구사항

우선순위: **P0** = MVP 필수, **P1** = 1차 출시 포함, **P2** = 이후

### F1. 프로젝트 관리 (P0)
- 프로젝트 목록(카드: 제목·진행률·최근 수정일), 생성/복제/삭제(휴지통 30일 보관).
- **책 정보 입력 폼**: 책 제목, 부제, 저자명, 주제/분야, 집필 의도, 주요 대상 독자, 핵심 메시지, 참고 도서·경쟁 도서, 목표 총 페이지 수, 톤(예: 친근/전문/서정), 기타 요청.
- **문체 샘플**(P0 추가): 작가가 기존에 쓴 글 1~5편 붙여넣기/업로드(.txt/.docx/.hwpx) → AI가 **문체 프로필**(문장 길이, 어미(~다/~요), 비유 빈도, 자주 쓰는 표현, 금기 표현)을 추출해 저장. 작가가 프로필을 직접 수정 가능.
- 프로젝트 설정: 판형(A5 고정, 구조상 확장 가능), 여백, 글꼴 크기, 장 시작 위치(오른쪽 페이지 시작 on/off), 쪽 번호 시작 규칙.

### F2. AI 목차 설계 (P0)
- 책 정보 + 문체 프로필 기반으로 **장/절 목차 설계안** 생성.
- 해당 분야 **전문가 관점**, **최신 트렌드**, **독자 흥미 포인트(훅)**, **전체 서사 흐름(도입-전개-심화-정리)** 을 반영.
- 출력 보고서 구성: ① 설계 콘셉트 요약 ② 목차(장/절, 각 절의 한 줄 요지와 권장 분량) ③ 장별 설계 근거 ④ 독자 흥미 포인트 ⑤ 차별화 포인트 ⑥ 예상 총 페이지.
- **대안 생성**: "다른 구성으로 다시"(최대 3안 비교), 특정 장만 재설계.
- 보고서는 프로젝트에 저장되어 언제든 다시 열람 가능.
- AI 응답은 JSON 스키마로 받아 파싱(파싱 실패 시 1회 자동 재시도 → 실패 시 원문 표시).

### F3. 목차 편집 (P0)
- 트리 구조(장 > 절, 선택적으로 소절 3단계까지) 편집: 추가 / 이름 변경(인라인) / 삭제(확인 모달, 본문 있으면 경고) / **드래그 앤 드롭 순서 변경**.
- 장/절 번호 자동 재번호(표기 형식 선택: `1장 / 1.1`, `제1장 / 01` 등).
- 절마다 상태 배지: `빈 페이지 / 스케치 / AI 초안 / 수정 중 / 교정 완료` + 분량 진행(예: 2.4/3p).
- 앞붙이(표제지·판권면·머리말·목차)와 뒷붙이(맺음말·참고문헌·부록) 항목 기본 제공 (P1).

### F4. 레이아웃 & 네비게이션 (P0)
- 3분할 레이아웃: **좌측 목차 패널**(접기 가능) | **중앙 편집기** | 우측 보조 패널(스케치/AI 설정/버전, 접기 가능).
- 편집기 상단 **브레드크럼**: `책 제목 > 3장 제목 > 3.2 절 제목`, 이전/다음 절 이동 버튼, 단축키(Ctrl+↑/↓).
- 현재 절이 전체 책에서 차지하는 **쪽 범위 표시**(예: p.45–48).

### F5. 편집기 (P0)
- 기본은 **빈 페이지**. 절 선택 시 활성화.
- 두 영역 구분:
  - **스케치 영역**: 작가의 개략 메모(불릿·키워드·에피소드·꼭 넣을 문장). 본문과 별도 저장되며 인쇄되지 않음.
  - **본문 영역**: 실제 원고. AI 생성 결과가 들어가고 작가가 직접 수정.
- 서식: 제목(절/소제목), 굵게·기울임, 인용구, 목록, 각주(P1), 구분선, 이미지.
- 편집기 폰트는 KoPub바탕체 Light로 **고정 세팅**, 본문 폭은 실제 조판 폭(103mm)과 동일 비율로 렌더링해 줄바꿈 체감이 인쇄와 같게.
- 글자 수(공백 포함/제외), 예상 페이지 수 실시간 표시.
- 실행 취소/다시 실행, 찾기·바꾸기(책 전체 범위 옵션, P1).

### F6. AI 집필하기 (P0 — 핵심)
- 편집기 **우측 상단 [집필하기]** 버튼 + 분량 입력(숫자 + 단위 `페이지`, 기본값은 목차 설계의 권장 분량).
- 입력 컨텍스트(서버에서 조립):
  1. `instruction.md` 전문 (**필수, 항상 포함**)
  2. 책 정보(제목·주제·집필 의도·대상 독자·톤)
  3. 문체 프로필 + 문체 샘플 발췌
  4. 전체 목차 + 현재 절 위치
  5. **직전 절 요약들**(앞 내용과의 연결) + 직전 절 마지막 2~3문단 원문
  6. 다음 절의 요지(다음으로 넘어갈 여지를 남기기 위해)
  7. 용어집/인물 설정(F11)
  8. 현재 절 스케치 + 목표 글자 수(= 페이지 × 환산계수)
- **스트리밍 출력**으로 편집기에 실시간 표시, [중지] 버튼.
- 본문에 기존 내용이 있으면 선택: `덮어쓰기 / 뒤에 이어쓰기 / 새 버전으로 생성(비교)`.
- 생성 후 분량 검증: 목표 대비 ±15% 벗어나면 "분량 맞추기(늘리기/줄이기)" 원클릭 제안.
- 생성 완료 시 해당 절 **요약(3~5문장)** 을 자동 생성·저장 → 이후 절 집필 컨텍스트로 사용.
- **부분 AI 기능**(P1): 선택 영역 → `다듬기 / 늘리기 / 줄이기 / 톤 바꾸기 / 예시 추가`.

### F7. 교정·교열 (P0)
- **[교정·교열]** 버튼 → 현재 절 본문 전체에 일괄 적용.
- 범위: 띄어쓰기, 맞춤법, 문장부호, 비문·중복 표현, 외래어 표기, 표기 통일(용어집 기준), 숫자·단위 표기. 기준은 국립국어원 한글 맞춤법·표준어 규정.
- 원칙: **의미와 문체는 바꾸지 않는다.** (교열 강도 옵션: `교정만 / 교정+가벼운 교열`)
- 적용 방식: 일괄 적용 후 **변경 내역 패널**(원문→수정, 사유)을 보여주고 개별 되돌리기 + 전체 되돌리기 제공. 적용 직전 상태는 자동 버전으로 저장.
- 긴 절은 문단 단위로 분할 요청 후 병합(토큰 한도 대응).

### F8. 자동 저장 & 버전 (P0)
- 입력 멈춘 후 **1초 디바운스 저장**, 최대 5초 간격 강제 저장, 창 닫기/탭 전환 시 즉시 저장.
- 저장 상태 표시: `저장 중… / 저장됨 14:32 / 오프라인 — 로컬에 보관 중`.
- 오프라인·서버 오류 시 IndexedDB에 임시 보관 → 복구 시 자동 동기화.
- **버전 기록**: AI 생성 전, 교정 적용 전, 수동 스냅샷 시 자동 생성. 버전 목록 → 비교(diff) → 복원.
- 프로젝트 전체 백업/복원(.json + 이미지 zip) (P1).

### F9. 페이지네이션 & 펼침면 미리보기 (P0)
- 책 전체를 실제 조판 규칙(3장)으로 페이지 분할 → **자동 쪽 번호** 부여.
- 보기 모드:
  - **편집 모드**: 연속 스크롤 편집(페이지 경계선·쪽 번호를 가이드로 오버레이)
  - **펼침면 모드**: 독자가 책을 펼쳤을 때처럼 `[왼쪽=짝수 | 오른쪽=홀수]` 2쪽 표시, 1쪽은 오른쪽 단독. 이전/다음 펼침면 넘기기.
- 가이드 오버레이 토글: 재단선, 재단 여백(3mm), 안전 영역, 본문 영역.
- 현재 편집 중인 페이지가 **왼쪽인지 오른쪽인지** 표시(안쪽 여백 방향 안내 포함).
- "장은 오른쪽 페이지에서 시작" 옵션 켜면 필요 시 빈 쪽 자동 삽입.
- 앞붙이는 쪽 번호 미표기, 본문 1장부터 1쪽(설정 가능).

### F10. 이미지 삽입 (P0)
- 드래그&드롭 / 업로드(JPG, PNG, WEBP), 서버 저장.
- 배치: `본문 폭 맞춤 / 크기 지정(mm) / 풀페이지 / 풀블리드(재단 여백까지 확장)`, 정렬, 캡션(자동 번호 `그림 3-1`).
- **해상도 검사**: 인쇄 크기 기준 유효 DPI 계산 → 300 미만 경고, 150 미만 강한 경고.
- 풀블리드 이미지가 있으면 PDF 출력 크기를 154×216으로 강제.
- 흑백 인쇄 선택 시 흑백 미리보기(P1).

### F11. 책 바이블 — 용어집·설정 (P1, 일관성 필수 기능)
- 용어집(표기 통일: 예 "AI/인공지능"), 인물·장소 설정(소설/에세이), 금지 표현 목록.
- 집필·교정 프롬프트에 자동 주입.

### F12. 내보내기 (P0)
**PDF (부크크 제출용)**
- 문서 크기 154×216mm(기본) 또는 148×210mm, 맞쪽 여백, 쪽 번호, 글꼴 서브셋 임베딩, 이미지 300DPI 유지.
- 범위: 전체 / 특정 장 / 현재 절.
- 출력 전 **사전 점검(preflight)**: 판형 오차, 안전 영역 침범, 저해상도 이미지, 빈 절, 누락 글꼴 → 목록으로 보고.
- 총 페이지 수 짝수 맞춤 옵션(끝에 빈 쪽 추가).

**HWPX**
- OWPML 규격 HWPX 생성: 용지 154×216mm, 맞쪽, 여백(3.2절), 본문 KoPub바탕체 Light 10pt/160%, 장·절 제목 스타일, 쪽 번호 꼬리말, 이미지 포함.
- 한글(한컴오피스)에서 열었을 때 부크크 기본 서식과 동일한 여백이 나오는지 검증.

**기타 (P1)**: DOCX, 원고 텍스트(.md).

### F13. 설정 & AI 사용량 (P1)
- API 연결 상태 확인, 모델명·Base URL 표시(키는 표시하지 않음).
- 호출별 토큰 사용량·누적 비용 추정, 실패 로그.
- `instruction.md` 앱 내 열람·편집(편집 시 파일에 반영, 버전 관리).

---

## 5. 비기능 요구사항

| 구분 | 요구사항 |
|---|---|
| 보안 | **API 키는 서버 환경변수(`.env`)에만 저장**, 클라이언트 번들·로그·Git에 노출 금지. 모든 AI 호출은 서버 라우트 경유. `.env`는 `.gitignore` 처리 |
| 성능 | 절 전환 ≤300ms, 300쪽 책 전체 페이지네이션 ≤5초(웹 워커/증분 계산), 첫 토큰 표시 ≤5초 |
| 안정성 | AI 호출 타임아웃 120초, 429/5xx 지수 백오프 재시도 최대 3회, 중단 시 부분 결과 보존 |
| 데이터 | 로컬 SQLite 파일 + 이미지 폴더. 모든 쓰기는 트랜잭션. 일일 자동 백업 7개 보관 |
| 접근성 | 키보드만으로 목차 이동·집필·저장 가능, 대비 AA |
| 폰트 라이선스 | KoPub 서체는 사용자가 설치/제공한 파일을 `public/fonts`에 배치. 저장소에 폰트 파일 커밋 금지(라이선스 확인 후 결정) |
| 브라우저 | 최신 Chrome/Edge 우선 |

---

## 6. 기술 설계 (권장안)

### 6.1 스택
| 레이어 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | **Next.js (App Router) + TypeScript** | UI와 서버 API 라우트를 한 프로젝트로 |
| UI | Tailwind CSS + shadcn/ui, dnd-kit(목차 드래그) | 빠른 구현 |
| 편집기 | **Tiptap(ProseMirror)** | 스키마 제어, 이미지·각주 확장, JSON 저장 |
| 상태/저장 | Zustand + 디바운스 저장, IndexedDB(오프라인 버퍼) | 자동 저장 |
| DB | **SQLite + Prisma** (추후 Postgres 전환 가능) | 1인 로컬 실행 |
| 페이지네이션/미리보기 | **Paged.js** (CSS Paged Media: `@page`, `:left/:right`, `bleed`, `marks`) | 편집 화면과 PDF가 같은 CSS로 조판 |
| PDF | **Playwright(Chromium) `page.pdf()`** 서버 렌더링 + Paged.js | 폰트 임베딩·mm 정밀도 |
| HWPX | JSZip + OWPML XML 템플릿 직접 생성 | 표준 규격 zip 구조 |
| AI | OpenAI 호환 REST (`fetch`, SSE 스트리밍) | 지정 엔드포인트 준수 |

### 6.2 AI 연동
```
POST {LLM_BASE_URL}/v1/chat/completions
Authorization: Bearer {LLM_API_KEY}
{
  "model": "claude-fable-5-1",
  "messages": [{"role":"system","content":"..."},{"role":"user","content":"..."}],
  "stream": true,
  "max_tokens": ...,
  "temperature": 0.7          // 집필 0.7 / 목차 0.8 / 교정 0.1
}
```
- 환경변수: `LLM_BASE_URL`, `LLM_API_KEY`, `LLM_MODEL=claude-fable-5-1`
- ⚠️ **Base URL(게이트웨이 호스트)은 아직 정해지지 않았다 → 확인 필요.**
- 최대 출력 토큰: 목표 글자 수 × 1.6 + 여유분(한국어 1자 ≈ 1~1.5 토큰 가정, 실측 후 보정). 10페이지 이상은 **소제목 단위 분할 생성**(개요 → 파트별 생성 → 연결부 다듬기).
- 서버 모듈 `lib/ai/`에 목적별 함수(`designToc`, `writeSection`, `proofread`, `summarize`, `analyzeStyle`, `rewriteSelection`)와 프롬프트 빌더 분리. 프롬프트 원문은 `prompts/` 폴더 파일로 관리.
- `instruction.md`는 **매 집필·교정 호출 시 파일에서 읽어** system 프롬프트에 포함(수정 즉시 반영, 캐시는 mtime 기준).

### 6.3 데이터 모델 (요약)
```
Project   { id, title, subtitle, author, topic, intent, audience, keyMessage, tone,
            references, targetPages, extra, styleProfile(JSON), styleSamples,
            layoutSettings(JSON), createdAt, updatedAt, deletedAt }
TocReport { id, projectId, version, conceptSummary, rationale(JSON), rawJson, createdAt }
Chapter   { id, projectId, order, title, kind(front|body|back) }
Section   { id, chapterId, order, title, gist, targetPages, status,
            sketch(JSON), content(JSON, Tiptap), summary, charCount, updatedAt }
Version   { id, sectionId, reason(ai_write|proofread|manual|restore), content(JSON), createdAt }
Asset     { id, projectId, filename, mime, widthPx, heightPx, path }
Glossary  { id, projectId, term, preferred, note }
AiLog     { id, projectId, purpose, promptTokens, completionTokens, ms, status, createdAt }
```

### 6.4 주요 API 라우트
| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/projects` | 프로젝트 생성 |
| POST | `/api/projects/:id/toc/design` | AI 목차 설계(보고서 포함) |
| PATCH | `/api/projects/:id/toc` | 목차 트리 일괄 저장(순서 변경 포함) |
| GET/PUT | `/api/sections/:id` | 절 조회/자동 저장 |
| POST | `/api/sections/:id/write` | AI 집필(SSE 스트림) |
| POST | `/api/sections/:id/proofread` | 교정·교열(변경 목록 반환) |
| GET/POST | `/api/sections/:id/versions` | 버전 목록/복원 |
| POST | `/api/assets` | 이미지 업로드 |
| POST | `/api/projects/:id/export/pdf` | PDF 생성 |
| POST | `/api/projects/:id/export/hwpx` | HWPX 생성 |
| POST | `/api/projects/:id/style/analyze` | 문체 샘플 분석 |

### 6.5 조판 CSS 핵심 (편집기·미리보기·PDF 공통)
```css
@page {
  size: 154mm 216mm;          /* 재단 여백 포함 */
  margin-top: 25mm;           /* 18 + 머리말 7 */
  margin-bottom: 31mm;        /* 18 + 꼬리말 13 */
}
@page :right { margin-left: 28mm; margin-right: 23mm;
  @bottom-right { content: counter(page); } }
@page :left  { margin-left: 23mm; margin-right: 28mm;
  @bottom-left  { content: counter(page); } }
@page :first { @bottom-right { content: none; } }
body { font-family: "KoPub Batang Light", "KoPubBatang", serif; font-size: 10pt;
       line-height: 1.6; text-align: justify; word-break: keep-all; orphans: 2; widows: 2; }
```
> 풀블리드 이미지는 페이지 박스 기준 음수 마진으로 154×216 전체를 채운다. 148×210 정사이즈 출력 시 `size: 148mm 210mm` + 여백 각 3mm 감산.

---

## 7. 화면 구성 (와이어프레임 설명)

```
┌───────────────────────────────────────────────────────────────────────┐
│ [≡] 책 제목            3장 제목 > 3.2 절 제목  ◀ ▶   저장됨 14:32  [보기▾]│
├──────────────┬─────────────────────────────────────┬──────────────────┤
│ 목차     [+] │  p.45–48 · 오른쪽 페이지부터 시작    │ ▸ 스케치          │
│ ▾ 1장 …      │  분량 [ 3 ] 페이지  [교정·교열] [집필하기] │ ▸ AI 옵션(톤 등) │
│   1.1 … ●    │ ┌───────────────────────────────┐ │ ▸ 버전 기록       │
│   1.2 … ○    │ │   (A5 본문 폭, KoPub바탕체)     │ │ ▸ 변경 내역(교정) │
│ ▾ 3장 …      │ │   빈 페이지 / 본문             │ │                  │
│   3.2 … ◐ ◀  │ │   ─ ─ ─ ─ 46 ─ ─ ─ ─ (쪽 경계) │ │                  │
│              │ └───────────────────────────────┘ │                  │
│ 진행 42/180p │  1,982자 · 약 2.8p / 목표 3p        │                  │
└──────────────┴─────────────────────────────────────┴──────────────────┘
```
- **[보기▾]**: 편집 모드 / 펼침면 모드 / 가이드 표시 토글
- 첫 진입(프로젝트 생성 직후): 책 정보 폼 → 목차 설계 보고서 화면 → [이 목차로 시작] 버튼

---

## 8. 마일스톤

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| M1 기반 | 프로젝트 CRUD, 책 정보 폼, DB, 3분할 레이아웃, 목차 트리 편집 | 목차 수동 편집·저장 |
| M2 AI 코어 | AI 클라이언트(스트리밍), 목차 설계 보고서, 집필하기, 절 요약, instruction.md 연동 | 스케치 → 3p 초안 생성 |
| M3 편집 품질 | 자동 저장(오프라인 버퍼), 버전 기록, 교정·교열 + 변경 내역, 문체 프로필 | 교정 적용/되돌리기 |
| M4 조판 | Paged.js 페이지네이션, 쪽 번호, 펼침면 모드, 가이드, 이미지 삽입·DPI 검사 | 펼침면에서 좌우 여백 정확 |
| M5 출력 | PDF(preflight 포함), HWPX | 부크크 규격체크 통과, 한글에서 서식 일치 |
| M6 다듬기 | 용어집, 부분 AI 기능, 사용량 화면, 백업 | — |

---

## 9. 수용 기준 (Acceptance Criteria)

1. 프로젝트 생성 후 책 정보를 입력하면 60초 이내 목차 보고서(장/절 + 근거 + 흥미 포인트)가 표시된다.
2. 목차에서 절을 추가·삭제·이름변경·드래그 이동하면 즉시 저장되고 번호가 재정렬된다.
3. 절 클릭 시 우측 편집기가 해당 절로 전환되고 브레드크럼이 `N장 > N.M절`을 정확히 표시한다.
4. 스케치 입력 + 분량 3p + [집필하기] → 스트리밍 생성, 결과 조판 페이지 2.55~3.45p.
5. 모든 집필·교정 요청의 system 프롬프트에 `instruction.md` 전문이 포함된다(AiLog로 확인 가능).
6. 타이핑 후 2초 내 "저장됨" 표시, 브라우저 강제 종료 후 재접속해도 마지막 입력이 남아 있다.
7. [교정·교열] 후 변경 목록이 표시되고 개별/전체 되돌리기가 동작한다.
8. 펼침면 모드에서 짝수 쪽은 왼쪽, 홀수 쪽은 오른쪽에 표시되며 안쪽 여백(28mm)이 제본 쪽에 위치한다.
9. 편집기·미리보기·PDF 모두 본문 글꼴이 KoPub바탕체 Light이며, PDF 글꼴 속성에 임베딩으로 표시된다.
10. 내보낸 PDF를 부크크 자동 규격체크(A5)에 올리면 판형 오류가 없다(154×216mm).
11. 내보낸 HWPX를 한글에서 열면 용지 154×216mm, 맞쪽, 여백 28/23/18/18, 머리말 7, 꼬리말 13으로 나온다.
12. 300DPI 미만 이미지 삽입 시 경고가 표시된다.
13. API 키가 브라우저 개발자도구(네트워크·소스)에서 보이지 않는다.

---

## 10. 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| 한국어 조판(자간·줄바꿈)이 HWP와 브라우저에서 달라 분량이 어긋남 | 조판 기준은 브라우저(Paged.js)로 일원화, HWPX는 "편집용 원고"로 정의. 환산계수 자동 보정 |
| 긴 분량 생성 시 출력 토큰 한도·품질 저하 | 소제목 단위 분할 생성, 연결부 다듬기 패스 |
| AI가 문체를 흉내 내지 못함 | 문체 프로필 + 샘플 발췌를 few-shot으로 주입, 작가 수정본을 새 샘플로 누적(P2) |
| 사실 오류(최신 트렌드 등) | 목차·본문에 "확인 필요" 표시 규칙(`instruction.md`), 수치·통계는 출처 요청 표기 |
| KoPub 폰트 파일 미설치 | 앱 시작 시 폰트 로드 검사, 미설치 시 안내 배너 + PDF 출력 차단 |
| API 키 유출 | 서버 전용 환경변수, 키 교체 절차 문서화 |

---

## 11. 확인이 필요한 사항 (Open Questions)
1. **`LLM_BASE_URL`**: `/v1/chat/completions`를 제공하는 호스트 주소(사내 게이트웨이/프록시 등). >> https://gw.letsur.ai
[참고]
필드	값
Inference provider	Gateway
Gateway base URL	https://gw.letsur.ai (/v1을 붙이지 않습니다)
Gateway API key	Letsur AI 게이트웨이 API 키
Credential kind	Static API key
Gateway auth scheme	x-api-key 
2. 일단 로컬 전용 실행으로 하자.
3. 앞붙이(판권면) 기본 문구, ISBN 표기 여부. 아래참조 (샘플)
--------
어린 왕자(제목을 적어주세요)

지은이  생텍쥐 페리(저자명, 필명을 적어주세요)

발  행  2025년 00월 00일
펴낸이  한건희
펴낸곳  주식회사 부크크
출판사등록  2014.07.15.(제2014-16호)
주  소  서울특별시 금천구 가산디지털1로 119 SK트윈타워 A동 305호
전  화  1670-8316
이메일  info@bookk.co.kr

ISBN  979-11-12-00000-0

www.bookk.co.kr
ⓒ 생텍쥐 페리 2025
본 책은 저작자의 지적 재산으로서 무단 전재와 복제를 금합니다. 
-------------
4. 본문 인쇄 흑백이다.
5. 소절(3단계 목차) 필요 없음
